import { z } from "zod";

export const createBrandSchema = z.object({
  name: z
    .string()
    .min(1, "لطفا نام برند را وارد نمایید.")
    .min(3, "نام برند باید حداقل 3 و حداکثر 50 کاراکتر باشد.")
    .max(50, "نام برند باید حداقل 3 و حداکثر 50 کاراکتر باشد."),

  slug: z
    .string()
    .min(1, "لطفا اسلاگ را وارد نمایید.")
    .min(3, "اسلاگ باید حداقل 3 و حداکثر 100 کاراکتر باشد.")
    .max(100, "اسلاگ باید حداقل 3 و حداکثر 100 کاراکتر باشد."),
});

export const updateBrandSchema = z.object({
  id: z.number().min(1),

  name: z
    .string()
    .min(1, "لطفا نام برند را وارد نمایید.")
    .min(3, "نام برند باید حداقل 3 و حداکثر 50 کاراکتر باشد.")
    .max(50, "نام برند باید حداقل 3 و حداکثر 50 کاراکتر باشد."),

  slug: z
    .string()
    .min(1, "لطفا اسلاگ را وارد نمایید.")
    .min(3, "اسلاگ باید حداقل 3 و حداکثر 100 کاراکتر باشد.")
    .max(100, "اسلاگ باید حداقل 3 و حداکثر 100 کاراکتر باشد."),
});
