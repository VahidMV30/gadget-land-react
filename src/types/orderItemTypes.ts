export type OrderItemResponse = {
  id: number;
  productName: string;
  productSlug: string;
  quantity: number;
  unitPrice: string;
  unitDiscount: string;
  totalDiscountAmount: string;
  subtotalAmount: string;
  totalAmount: string;
};
